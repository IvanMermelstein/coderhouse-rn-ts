import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    textInput: {
        borderColor: '#b18fcf',
        borderBottomWidth: 1,
        marginBottom: 10,
        width: '70%',
        height: 40,
        fontSize: 14,
        color: '#978897'
    }
});
